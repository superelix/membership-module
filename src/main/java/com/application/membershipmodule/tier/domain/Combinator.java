package com.application.membershipmodule.tier.domain;

/** docs/prd/02-membership-tiers.md §4 — ANY (OR) / ALL (AND) combination across a tier's criteria. */
public enum Combinator {
    ANY,
    ALL
}
